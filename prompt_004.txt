Only 3 small changes on the migrations: 1) remove the sample data migration; 2) use the following format based on current timestamp on the migrations: YYYYMMDDHHmmss_name_of_the_migration.sql; 3) add a `migration` target on the Makefile so it is possible to create new migration files (with the correct filename format) containing an initial template with two comments: -- +migrate Up and -- +migrate Down