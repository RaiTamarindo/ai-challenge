<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive data from cloud backup -->
        <exclude domain="sharedpref" path="FeatureVotingPrefs.xml"/>
    </cloud-backup>
    <device-transfer>
        <!-- Include app data but exclude credentials -->
        <exclude domain="sharedpref" path="FeatureVotingPrefs.xml"/>
    </device-transfer>
</data-extraction-rules>